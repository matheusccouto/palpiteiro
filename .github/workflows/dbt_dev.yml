name: dbt
on:
  push:
    branches:
      - dev
    paths:
      - dbt
jobs:
  dbt:
    uses: ./.github/workflows/dbt.yml
    with:
      TARGET: dev
    secrets:
      GCP_KEYFILE: ${{ secrets.GCP_KEYFILE_DEV }}